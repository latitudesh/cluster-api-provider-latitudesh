---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: latitudemachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: LatitudeMachineTemplate
    listKind: LatitudeMachineTemplateList
    plural: latitudemachinetemplates
    shortNames:
    - latmt
    singular: latitudemachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              template:
                properties:
                  spec:
                    properties:
                      operatingSystem:
                        description: OperatingSystem is the operating system slug
                          for the server
                        minLength: 1
                        type: string
                      plan:
                        description: Plan is the server plan slug
                        minLength: 1
                        type: string
                      providerID:
                        description: ProviderID is the unique identifier as specified
                          by the cloud provider
                        type: string
                      sshKeySecretRef:
                        description: |-
                          SSHKeySecretRef references a Kubernetes Secret containing SSH key IDs.
                          The Secret must contain a key with a comma-separated list of SSH key IDs.
                          Example: "ssh_abc123,ssh_def456"
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - operatingSystem
                    - plan
                    type: object
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
