---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: metallbconfigs.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: MetalLBConfig
    listKind: MetalLBConfigList
    plural: metallbconfigs
    shortNames:
    - mlb
    singular: metallbconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.installed
      name: Installed
      type: boolean
    - jsonPath: .status.controlPlaneEndpoint
      name: Endpoint
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MetalLBConfig is the Schema for the metallbconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MetalLBConfigSpec defines the desired state of MetalLBConfig
            properties:
              clusterRef:
                description: ClusterRef references the LatitudeCluster this MetalLB
                  configuration serves
                properties:
                  name:
                    description: Name is the name of the LatitudeCluster
                    type: string
                  namespace:
                    description: Namespace is the namespace of the LatitudeCluster
                      (defaults to the same namespace)
                    type: string
                required:
                - name
                type: object
              controlPlaneEndpoint:
                description: |-
                  ControlPlaneEndpoint configures if a LoadBalancer Service should be created
                  for the Kubernetes API server
                properties:
                  enabled:
                    default: true
                    description: Enabled controls whether to create a LoadBalancer
                      Service for the API server
                    type: boolean
                  ipAddress:
                    description: |-
                      IPAddress is the specific IP to assign (must be within the pool)
                      If not specified, an IP is automatically allocated
                    type: string
                  port:
                    default: 6443
                    description: 'Port is the port for the Kubernetes API server (default:
                      6443)'
                    format: int32
                    type: integer
                required:
                - enabled
                type: object
              ipAddressPool:
                description: IPAddressPool defines the pool of IP addresses that MetalLB
                  can allocate
                properties:
                  addresses:
                    description: |-
                      Addresses is a list of IP address ranges in CIDR notation or range format
                      Examples: "192.168.1.240/28", "192.168.1.240-192.168.1.250"
                    items:
                      type: string
                    type: array
                  autoAssign:
                    default: true
                    description: AutoAssign controls whether IP addresses are automatically
                      assigned from this pool
                    type: boolean
                  avoidBuggyIPs:
                    default: false
                    description: AvoidBuggyIPs prevents allocating IPs ending in .0
                      or .255
                    type: boolean
                  name:
                    description: Name is the name of the IP address pool
                    type: string
                required:
                - addresses
                - name
                type: object
              l2Advertisements:
                description: L2Advertisements defines Layer 2 advertisement configuration
                items:
                  description: L2Advertisement defines how to advertise IP addresses
                    via Layer 2
                  properties:
                    interfaces:
                      description: |-
                        Interfaces is a list of network interfaces to advertise on
                        If empty, all interfaces are used
                      items:
                        type: string
                      type: array
                    ipAddressPools:
                      description: IPAddressPools is a list of IP address pool names
                        to advertise
                      items:
                        type: string
                      type: array
                    name:
                      description: Name is the name of the L2 advertisement
                      type: string
                    nodeSelectors:
                      description: NodeSelectors allows limiting which nodes can advertise
                        IPs
                      items:
                        description: |-
                          A label selector is a label query over a set of resources. The result of matchLabels and
                          matchExpressions are ANDed. An empty label selector matches all objects. A null
                          label selector matches no objects.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      type: array
                  type: object
                type: array
              version:
                default: v0.14.3
                description: 'Version is the MetalLB version to install (default:
                  v0.14.3)'
                type: string
            required:
            - clusterRef
            - ipAddressPool
            type: object
          status:
            description: MetalLBConfigStatus defines the observed state of MetalLBConfig
            properties:
              allocatedIPs:
                description: AllocatedIPs tracks IPs allocated from the pool
                items:
                  type: string
                type: array
              conditions:
                description: Conditions defines current service state of the MetalLBConfig
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              controlPlaneEndpoint:
                description: ControlPlaneEndpoint is the LoadBalancer IP assigned
                  to the API server
                type: string
              installed:
                description: Installed indicates that MetalLB has been installed in
                  the workload cluster
                type: boolean
              ready:
                description: Ready indicates that MetalLB is installed and configured
                type: boolean
              version:
                description: Version is the installed MetalLB version
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
