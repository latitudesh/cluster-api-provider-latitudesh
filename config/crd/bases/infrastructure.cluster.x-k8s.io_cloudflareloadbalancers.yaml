---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cloudflareloadbalancers.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: CloudflareLoadBalancer
    listKind: CloudflareLoadBalancerList
    plural: cloudflareloadbalancers
    shortNames:
    - cflb
    singular: cloudflareloadbalancer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.hostname
      name: Hostname
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CloudflareLoadBalancer is the Schema for the cloudflareloadbalancers
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareLoadBalancerSpec defines the desired state of CloudflareLoadBalancer
            properties:
              accountId:
                description: AccountID is the Cloudflare Account ID (required for
                  load balancer pools)
                type: string
              clusterRef:
                description: ClusterRef references the LatitudeCluster this load balancer
                  serves
                properties:
                  name:
                    description: Name is the name of the LatitudeCluster
                    type: string
                  namespace:
                    description: Namespace is the namespace of the LatitudeCluster
                      (defaults to the same namespace)
                    type: string
                required:
                - name
                type: object
              credentialsRef:
                description: CredentialsRef references the Secret containing Cloudflare
                  API credentials
                properties:
                  name:
                    description: Name is the name of the Secret containing Cloudflare
                      credentials
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Secret (defaults
                      to the same namespace as CloudflareLoadBalancer)
                    type: string
                required:
                - name
                type: object
              healthCheckInterval:
                default: 60
                description: 'HealthCheckInterval is the interval between health checks
                  in seconds (default: 60)'
                format: int32
                type: integer
              healthCheckPath:
                default: /healthz
                description: 'HealthCheckPath is the path used for health checks (default:
                  /healthz)'
                type: string
              hostname:
                description: Hostname is the DNS name for the load balancer (e.g.,
                  "api.mycluster.example.com")
                type: string
              port:
                default: 6443
                description: 'Port is the port that the load balancer will listen
                  on (default: 6443 for K8s API)'
                format: int32
                type: integer
              proxied:
                default: false
                description: 'Proxied indicates if traffic should be proxied through
                  Cloudflare (default: false for K8s API)'
                type: boolean
              ttl:
                default: 120
                description: 'TTL is the DNS TTL in seconds (default: 120)'
                format: int32
                type: integer
              zoneId:
                description: ZoneID is the Cloudflare Zone ID where the load balancer
                  will be created
                type: string
            required:
            - accountId
            - clusterRef
            - credentialsRef
            - hostname
            - zoneId
            type: object
          status:
            description: CloudflareLoadBalancerStatus defines the observed state of
              CloudflareLoadBalancer
            properties:
              conditions:
                description: Conditions defines current service state of the CloudflareLoadBalancer
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dnsRecordId:
                description: DNSRecordID is the Cloudflare DNS record ID
                type: string
              endpoint:
                description: Endpoint is the fully qualified domain name of the load
                  balancer
                type: string
              loadBalancerId:
                description: LoadBalancerID is the Cloudflare Load Balancer ID
                type: string
              origins:
                description: Origins is the list of control plane origins registered
                  with the load balancer
                items:
                  description: OriginStatus represents the status of a single origin
                    (control plane node)
                  properties:
                    address:
                      description: Address is the IP address of the origin
                      type: string
                    healthy:
                      description: Healthy indicates if the origin is passing health
                        checks
                      type: boolean
                    name:
                      description: Name is the name of the origin (typically the machine
                        name)
                      type: string
                  required:
                  - address
                  - healthy
                  - name
                  type: object
                type: array
              poolId:
                description: PoolID is the Cloudflare Pool ID containing the origins
                type: string
              ready:
                description: Ready indicates that the load balancer is fully configured
                  and operational
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
